# Collate Sources — Streamlit app

Streamlit app to collate CSV/XLSX, dedupe by Title and generate TF‑IDF keyphrases.

Run locally:
1. python3 -m venv .venv
2. source .venv/bin/activate
3. pip install -r requirements.txt
4. streamlit run app.py

